import React from 'react';
import { View, Text, StyleSheet, SafeAreaView, Platform, StatusBar } from 'react-native';

export default function App() {
  return (
    <SafeAreaView style={styles.container}>
      <StatusBar 
        barStyle={Platform.OS === 'ios' ? 'dark-content' : 'light-content'} 
        backgroundColor="#059669"
      />
      <View style={styles.header}>
        <Text style={styles.headerText}>Configuração de OS</Text>
      </View>
      <View style={styles.content}>
        <Text>Você está rodando no: {Platform.OS}</Text>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
  header: {
    height: 80,
    ...Platform.select({
      ios: {
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center',
        borderBottomWidth: 1,
        borderBottomColor: '#ddd',
      },
      android: {
        backgroundColor: '#059669',
        justifyContent: 'center',
        alignItems: 'flex-start',
        paddingLeft: 20,
        elevation: 4,
      },
    }),
  },
  headerText: {
    fontSize: 18,
    fontWeight: 'bold',
    ...Platform.select({
      ios: {
        color: '#000',
      },
      android: {
        color: '#fff',
      },
    }),
  },
  content: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
